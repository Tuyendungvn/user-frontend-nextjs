export declare namespace IFindCompanyRoute {
  export interface Queries {
    page?: string | number;
    k?: string; // keywords
    p?: string; //province
  }

  export type FullQueries = Queries;
}
